apiVersion: v1
kind: Service
metadata:
  name: jenkins-svc
  namespace: user1-jenkins
spec:
  type: ClusterIP
  selector:
    app: jenkins
    owner: user1
  ports:
    - name: web
      port: 8080
      targetPort: 8080
    - name: agent
      port: 50000
      targetPort: 50000
